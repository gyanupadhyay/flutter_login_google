/// Central place for API-related constants (base URLs, endpoints, etc.).
///
/// Keeps data sources free of hardcoded URLs.
class ApiConstants {
  ApiConstants._();

  static const String jsonPlaceholderBaseUrl = 'https://jsonplaceholder.typicode.com';

  // Endpoints
  static const String posts = '/posts';
}

